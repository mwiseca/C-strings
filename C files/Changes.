The C files are files used to build the program. They were added to the main when they worked.
March 27 2023
Moved the choice function above main. More comments added and memory allocation to scanf reduced.
March 29 2023.
Added  clear input buffer works with some compilers.
April 30 2023.
Added __fpurge(stdin); and fflush(stdin); they will work with different compilers.
May 4 2023.
Arrays file was added.
September 24 2023.
A stand alone fgets with the strlen function to remove the newline character was added.
October 9 2023.
The old Cstrings files have been deleted they are still in the dev branch.
April 24 2024.
Two files were added using functions for all choices. One with functions above main.
The other below main.
April 24 2024.
A new file using functions for choices was added. It consolidates heap heap numbers and check malloc
into one. Else if statements is accessible from if without going through main.
May 14 2024.
The Cstringsc.c file was deleted. Cstrings.b.c had if to else if and strings added.
Check ifStatement to see the code.
check i in choices to use.
The heap function consolidates heap and heap numbers and has direct access to check malloc.
Check h in choices.
Check heap to see the code.
may 16 2014.
A stand alone malloc file was added. It includes a check malloc option.
May 18 2024.
A file was added arrays for ints or doubles. this may be added to the program.
numarray.c
September 11 2024.
Cstrings.a.c was formated for easer reading.
November 12 2024.
Cstrings.b.c was formated for easer reading.
November 12 2024.
Cstrings.a.c and Cstrings.b.c was formated better.
November 14 2024.
A stand alone multiconditions file has been added.
It makes if and else if statements with 2 conditions.
December 20 2024.
Cstrings limit.c was added to C files.
It limits the size of all user inputs
for devices with very low memory.
febuary 2 2025.
A fgetsScanf was added. It allocates low memory and uses a while loop in function to flush input buffer.
It uses scanf that tells the user if wrong data type is entered. There is comment explaining how it works.
Febuary 8 2025.
CstringsLimmit.c has been changed so users must add a number to limmit size on scanf for strings.
Check s and sb in Choices.
Febuary 27 2025.
CstringsLimit.c has been changed to allow for easer changing Memory size for most user inputs.
It uses #define SIZE and define MAX.
March 12 2025.
A new file was added textToBin.c It converts keyboard characters to binary and hexadecimal numbers.
Right now theres enough to make sentances in binary and hexadecimal.
September 8 2025.
The break statement on line 226 was removed.
It was used to fix another error.
September 10 2025;
